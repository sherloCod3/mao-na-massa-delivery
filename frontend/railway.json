{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "mv node_modules/.vite node_modules/.vite.bak 2>/dev/null; npm ci; rm -rf node_modules/.vite.bak 2>/dev/null; npm run build"
  },
  "deploy": {
    "static": true,
    "publishPath": "dist"
  }
}
