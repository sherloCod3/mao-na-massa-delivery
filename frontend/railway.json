{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "rm -rf node_modules/.vite node_modules/.cache && npm ci && npm run build"
  },
  "deploy": {
    "static": true,
    "publishPath": "dist"
  }
}
